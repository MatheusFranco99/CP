#include <iostream>
#include <stdio.h>
#include <stdlib.h>
#include <vector>
#include <utility>
#include <string>
#include <bits/stdc++.h>
#include <set>
#define ll long long
#define min(x,y) x < y ? x : y
#define max(x,y) x > y ? x : y

using namespace std;



void solve(){
    int n;
    cin >> n;
    int r = 1, k = 1;
    // survive until find line in which n will be 2^(line-1) - 1
    n--;
    while(n!=0){
        
    }
}

int main(){
        int t;
        cin >> t;
        for(int i = 1; i <= t; i++){
            cout << "Case #" << i << ":\n";
            solve();
        }
        return 0;
}