#include <iostream>
#include <stdio.h>
#include <stdlib.h>
#include <vector>
#include <bits/stdc++.h>
#include <set>
#define ll long long
#define min(x,y) x < y ? x : y
#define max(x,y) x > y ? x : y

using namespace std;

const int rMax = 20;
const int cMax = 20;
int m[rMax][cMax];


void solve(){
        int r,c;
        cin >> r >> c;
                
}

int main(){
        int t;
        cin >> t;
        int total = t;
        while(t-- > 0{
                cout << "Case " << total-t+1 << ":";
                solve();
        }
        return 0;
}